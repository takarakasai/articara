[package]
name = "articara-plugin-api"
version = "0.1.0"
edition = "2024"
description = "Shared types for articara WASM plugin protocol (View primitives, Request/Response envelopes)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
